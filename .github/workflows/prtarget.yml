name: PrTarget New

on:
  pull_request_target:

jobs:
  generator:
    runs-on: ubuntu-latest
    steps:
    - id: comment
      shell: bash
      run: |
        echo "I am very new."
        echo " ${{ github.event.action }}