name: pr

on:
  pull_request:

permissions:
  contents: write

jobs:
  comment:
    permissions:
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
    - run: echo "$EVENT_CONTEXT"
      env:
        EVENT_CONTEXT: ${{ toJson(github.event) }}
